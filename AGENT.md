# AGENT.md — 本项目 Coding Agent 行为规范

> 本文件约束所有在本仓库工作的 Coding Agent（Claude Code / Cursor / Codex）。
> 目标：让 AI 一次只完成一个 Phase，防止跨阶段开发、自作主张重构和技术范围膨胀。
> 本项目的开发节奏：AI 修改代码 → AI 测试 → AI 生成学习文档 → 用户阅读学习文档 → 用户确认 → 才进入下一 Phase。

## A. 阶段边界（最高优先级）

1. 一次只做当前 Phase 文档（docs/phases/phase-XX.md）中定义的验收目标。
2. 发现本 Phase 范围外的问题（bug、优化点、新想法）时：**不实现、不顺手修**，写入 docs/PROGRESS.md 的 Backlog 区即可。
3. 禁止新增依赖，除非当前 Phase 文档明确点名。想引入任何新框架/库，必须先停下来询问用户。
4. 禁止重构。代码改动只允许三种：
   a. 实现当前 Phase 的验收目标；
   b. 修复本次改动直接引起的错误；
   c. 修复构建失败 / 进程崩溃（见第 5 条）。
5. 冲突优先级：修复构建失败 / 进程崩溃 > 阶段边界规则。越界修复后必须在 docs/PROGRESS.md 记录越界内容与原因。

## B. 测试与验证

6. 每个验收目标必须有真实运行的测试或验证命令，并把真实输出粘贴进 Phase 文档。禁止只写"测试通过"而不贴输出。
7. 涉及 LLM 的改动，必须真实调用 API 验证至少一次成功路径 + 一次失败路径，两条输出都贴进 Phase 文档。

## C. 学习文档生成

8. 学习文档只写本 Phase 涉及的真实代码，引用代码片段时带文件路径和行号；禁止粘贴泛泛概念。
9. 学习文档面向**阅读学习**：每个 Phase 完成后，AI 按模板填写完整文档（真实代码片段 + 真实测试输出 + 真实踩坑记录）。模板中的"动手练习"与"本阶段必须会说的话"章节**删除或改写为阅读型说明**，不要求用户动手练习；"面试问题"章节必须附参考回答。
10. 完成"代码 + 测试 + 学习文档"后立即停止：输出本阶段总结，不做任何超出 Phase 的动作。

## D. 进程管理

11. docs/PROGRESS.md 中的 User Confirmation 字段只允许用户本人修改（PENDING → PASS + 日期）。AI 看到 PENDING 时，本 Phase 未结束，不得继续下一 Phase。
12. 每个 Phase 结束时执行 git commit，commit message 带 Phase 编号（如 `phase-00: 修复后端崩溃并跑通项目`）。
13. 本文件（AGENT.md）与 docs/ROADMAP.md 的修改必须由用户明确要求，AI 不得自行调整规则。

## 禁止写入的笼统规则

以下规则**不要**出现在本文件或任何 Phase 文档中——它们会被 AI 解读为重构授权或范围放大：
- "保持代码整洁 / 尽量优化代码"
- "测试覆盖率尽量高"
- "遇到不确定就咨询用户"（正确写法："只询问影响验收标准的决策"）

## 工具兼容性

- Claude Code 自动读取本文件。
- 若切换到 Cursor / Codex，需将本文件同步为 AGENTS.md（它们不读 AGENT.md）。
